<mapper namespace="product">

    <select id="selectList">
        select * from product
    </select>

    <select id="selectOne">
        select * from product where id = ? and product_name = ?
    </select>
</mapper>